sigma = 2;
width = sigma*6+1;
gaussBoth = fspecial('gaussian', width, sigma);
gaussX = fspecial('gaussian', [1,width], sigma);
gaussY = fspecial('gaussian', [width,1], sigma);

im2 = rgb2gray(im2double(imread('image2.png')));
%both
tic;
im2
toc;

%seperate
tic;

toc;

pause;